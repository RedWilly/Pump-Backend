# Generate the migration files
npx prisma migrate dev --name add_volume_tracking

# Apply the migration to your database
npx prisma migrate deploy

# Generate the Prisma Client with the new models
npx prisma generate